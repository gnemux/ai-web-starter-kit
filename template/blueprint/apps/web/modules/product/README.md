# Product-owned module

Place domain types, use cases, copy, assets, product events and product data adapters here. Product code may consume public `@xwlc/*` roots and documented `modules/platform` facades. Platform modules must never import this directory.
